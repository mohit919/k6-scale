{
  "name": "k6-scale",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Business-volume driven workload modelling for Grafana k6.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "find src profiles examples tests -name '*.js' -print0 | xargs -0 -n1 node --check",
    "plan": "node src/cli/plan.js profiles/retail-peak.js",
    "plan:json": "node src/cli/plan.js profiles/retail-peak.js --json"
  },
  "engines": {
    "node": ">=20"
  }
}
